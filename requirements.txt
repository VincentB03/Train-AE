# JAX and PyTorch are not listed: install them first, with the CUDA build
# matching the machine (see README), e.g. pip install -U "jax[cuda12]"

datasets==4.0.0
einops==0.8.2
equinox==0.13.8
flowjax
jax-galsim
jaxtyping==0.3.10
paramax
matplotlib==3.10.0
matplotlib-inline==0.2.2
matplotlib-venn==1.1.2
numpy>=2.1,<3
optax==0.2.8
PyYAML==6.0.3
tensorflow-datasets==4.9.10
tqdm==4.67.3
vega-datasets==0.9.0
wandb==0.27.0
# Only for experiments/verification.py (>=0.6: older versions call z_score_norm "whiten")
#pqm>=0.6
